import { Text, View } from "react-native";
import { styles } from "./MapaPrestadorScreenStyle";

export default function MapaPrestadorScreen(): JSX.Element {
    return (
        <View style={styles.container}>
            <Text> Tela de mapa do prestador </Text>
        </View>
    );
}