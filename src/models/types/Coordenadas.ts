export class Coordenadas {
    latitude?: number; // Latitude da localização
    longitude?: number; // Longitude da localização
    latitudeDelta?: number; // Delta de latitude para zoom vertical
    longitudeDelta?: number; // Delta de longitude para zoom horizontal
}